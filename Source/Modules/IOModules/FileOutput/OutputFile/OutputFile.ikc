<class name="OutputFile" description="writes its input to a file each tick">
    <parameter name="filename" type="string" default="output.csv" description="File to write inside UserData. Use # for an unpadded sequence number or multiple hashes such as #### for a fixed-width zero-padded number. Escape a literal hash as \#." />
    <parameter name="format" type="string" default="csv" description="Delimited text format: csv or tsv." />
    <parameter name="decimals" type="number" default="4" min="0" max="20" description="Number of digits after the decimal point in numeric columns." />
    <parameter name="timestamp" type="bool" default="yes" description="Include a per-file tick counter in the first column." />
    <parameter name="directory" type="string" default="" description="Create a new numbered directory on each run, such as recording.000. Leave empty to write directly inside UserData." />

    <input name="INPUT" description="Values written as one flattened row." flatten="true" use_label="yes" />
    <input name="WRITE" optional="yes" size="1" description="Write while greater than zero. When disconnected, write every tick." />
    <input name="NEWFILE" optional="yes" size="1" description="On a rising edge, open the next file selected by the filename # placeholder." />
</class>
