# Python Tests

This directory contains kernel tests for Python-backed Ikaros modules.

Run `python3 python_test.py` to check the Python tests. Python test module classes
are kept in the sibling `TestModules/PythonTestModules` directory.

Files with names starting with `test` are run by the script.
